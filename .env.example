# 复制为 .env.local 并填入。

# ── 模型(NPC 大脑)── 三件套,任何 OpenAI 兼容端点都行:
LLM_API_KEY=             # 服务商的 key(必填)
LLM_MODEL=              # 模型名,如 gpt-4o-mini / deepseek-chat / glm-4-flash(必填)
LLM_BASE_URL=           # 接口地址,如 https://api.deepseek.com/v1(选填,留空=OpenAI 官方)

# 可选:
# LLM_MAX_TOKENS=1024   # 思考/推理模型建议调高,如 2048
# LLM_TOOL_MODE=auto    # auto(默认) | tool(强制函数调用) | json(纯推理模型用)
# LLM_TIMEOUT_MS=50000  # 单次请求超时(毫秒)。建议略小于 vercel.json 的 maxDuration*1000,
#                       # 让慢端点优雅失败(前端提示"网络抽风"、不计回合),而非被平台硬超时
# LLM_MAX_RETRIES=0     # 失败重试次数(默认 0)。慢思考模型别调高,重试会叠加延迟撞函数上限
# LLM_EXTRA_BODY=       # 透传给模型接口的额外参数(JSON)。【慢思考模型最大提速手段:关思考】
#                       #   Vercel Hobby 函数上限 60s 无法突破,只能让单轮更快。任选其一试:
#                       #   GLM/智谱:  {"thinking":{"type":"disabled"}}
#                       #   vLLM/中转:  {"chat_template_kwargs":{"enable_thinking":false}}
#                       #   通用推理档: {"reasoning_effort":"low"}

# ── 排行榜/埋点存储 ── 本地可不填(用文件);部署到 serverless 必填:
# UPSTASH_REDIS_REST_URL=https://xxxx.upstash.io
# UPSTASH_REDIS_REST_TOKEN=xxxxxxxxxxxxxxxx
